<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newdoc2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".ElementMain"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".VidioPage" />
        <activity android:name=".VedioShow" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Info" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Balloon" />
        <activity android:name=".airshipActivity" />
        <activity android:name=".login" />
        <activity android:name=".regester" />
        <activity android:name=".FirstAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>